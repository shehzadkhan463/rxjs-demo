import { GetOptionsDirective } from './get-options.directive';

describe('GetOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new GetOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
