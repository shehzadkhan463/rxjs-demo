import { GetTemplateDirective } from './get-template.directive';

describe('GetTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new GetTemplateDirective<any>();
    expect(directive).toBeTruthy();
  });
});
